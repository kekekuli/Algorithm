#include <iostream>
#include <queue>
#include <string>
using namespace std;
priority_queue<int> lft, rht; 
int main(){
    int t;
    cin >> t;
    while(true){
        priority_queue<int>* ptr = &lft;
        string s;
        int k;
        cin >> s;
        cin >> k;
        for (int i = 0; i < s; i++){

        }
    }
}